<% content_for :page_title, t("views.exercises.custom.edit_title") %>

<div class="max-w-3xl mx-auto mt-16">

  <div class="card p-8">
    <h1 class="text-2xl font-bold text-ink-primary mb-8 flex items-center gap-3">
      <span class="w-8 h-8 rounded-lg bg-brand/15 flex items-center justify-center flex-shrink-0">
        <i class="fas fa-pen text-brand text-sm"></i>
      </span>
      <%= t("views.exercises.custom.edit_title") %>
    </h1>

    <%= render "form", exercise: @exercise %>
  </div>

</div>
