<div class="max-w-6xl mx-auto mt-16 card p-8">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-ink-primary"><%= t("views.recipes.new.title") %></h1>
  </div>
  <%= render "form", recipe: @recipe %>
</div>
