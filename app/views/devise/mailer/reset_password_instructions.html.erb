<p><%= t("views.devise.mailer.reset_password.greeting", email: @resource.email) %></p>

<p><%= t("views.devise.mailer.reset_password.intro") %></p>

<p><%= link_to t("views.devise.mailer.reset_password.action"), edit_password_url(@resource, reset_password_token: @token) %></p>

<p><%= t("views.devise.mailer.reset_password.ignore") %></p>
<p><%= t("views.devise.mailer.reset_password.no_change") %></p>
