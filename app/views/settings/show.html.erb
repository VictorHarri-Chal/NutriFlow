<% content_for :page_title, "Paramètres" %>

<div class="max-w-6xl mx-auto mt-16 bg-white rounded-xl shadow-md overflow-hidden" data-controller="tabs">
  <!-- Navigation par onglets -->
  <div class="border-b border-gray-200">
    <nav class="flex space-x-8 px-8 pt-6" aria-label="Onglets">
      <a href="#general" class="active border-b-2 border-blue-600 py-4 px-1 text-sm font-medium text-blue-600 hover:text-gray-700 hover:border-gray-300" data-tab="general" data-tabs-target="tab" data-action="click->tabs#switchTab">
        <i class="fas fa-cog mr-2"></i>
        Général
      </a>
      <%# <a href="#notifications" class="border-b-2 border-transparent py-4 px-1 text-sm font-medium text-gray-500 hover:text-gray-700 hover:border-gray-300" data-tab="notifications" data-tabs-target="tab" data-action="click->tabs#switchTab">
        <i class="fas fa-bell mr-2"></i>
        Notifications
      </a> %>
      <a href="#security" class="border-b-2 border-transparent py-4 px-1 text-sm font-medium text-gray-500 hover:text-gray-700 hover:border-gray-300" data-tab="security" data-tabs-target="tab" data-action="click->tabs#switchTab">
        <i class="fas fa-shield-alt mr-2"></i>
        Sécurité
      </a>
    </nav>
  </div>

  <!-- Contenu des onglets -->
  <div class="p-8">
    <%= render 'general' %>
    <%= render 'notifications' %>
    <%= render 'security' %>
  </div>
</div>
