<nav id="main-navbar"
     data-sidebar-target="navbar"
     class="h-16 bg-white shadow-sm border-b border-gray-200 fixed top-0 left-0 w-full md:pl-64 z-40">
  <div class="h-full py-0 flex items-center px-4">
    <button data-sidebar-target="desktopSideBarOpeningButton" data-action="click->sidebar#desktopToggle" class="mr-4 p-2 hidden">
      <i class="fas fa-bars text-blue-600 text-xl"></i>
    </button>

    <h1 class="text-2xl font-bold text-gray-800 ml-2 mr-auto">
      <%= yield :page_title %>
    </h1>

    <div class="flex items-center">
      <% if content_for?(:navbar_actions) %>
        <%= yield :navbar_actions %>
      <% end %>
    </div>
  </div>
</nav>
