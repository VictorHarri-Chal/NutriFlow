
<nav class="mt-6 flex flex-col h-full px-4">
    <div class="space-y-2">
        <%= link_to root_path, class: "flex items-center px-4 py-3 text-gray-700 hover:bg-blue-50 hover:text-blue-600 rounded-lg #{current_page?(root_path) ? 'bg-blue-100 text-blue-600' : ''}" do %>
            <i class="fas fa-home w-5 h-5 mr-3"></i>
            <span class="font-medium pb-1">Accueil</span>
        <% end %>

        <%= link_to calendars_path, class: "flex items-center px-4 py-3 text-gray-700 hover:bg-blue-50 hover:text-blue-600 rounded-lg #{current_page?(calendars_path) ? 'bg-blue-100 text-blue-600' : ''}" do %>
            <i class="fas fa-calendar-alt w-5 h-5 mr-3"></i>
            <span class="font-medium pb-1">Calendrier</span>
        <% end %>

        <%= link_to foods_path, class: "flex items-center px-4 py-3 text-gray-700 hover:bg-blue-50 hover:text-blue-600 rounded-lg #{current_page?(foods_path) ? 'bg-blue-100 text-blue-600' : ''}" do %>
            <i class="fas fa-utensils w-5 h-5 mr-3"></i>
            <span class="font-medium pb-1">Aliments</span>
        <% end %>

        <%= link_to daily_calorie_requirement_path, class: "flex items-center px-4 py-3 text-gray-700 hover:bg-blue-50 hover:text-blue-600 rounded-lg #{current_page?(daily_calorie_requirement_path) ? 'bg-blue-100 text-blue-600' : ''}" do %>
            <i class="fas fa-fire-flame-simple w-5 h-5 mr-3"></i>
            <span class="font-medium pb-1">Besoin calorique</span>
        <% end %>
    </div>
    <div class="mt-auto pt-4 border-t border-gray-100">
        <%= link_to setting_path, class: "flex items-center px-4 py-3 text-gray-700 hover:bg-blue-50 hover:text-blue-600 rounded-lg" do %>
            <i class="fas fa-cog w-5 h-5 mr-3"></i>
            <span class="font-medium pb-1">Paramètres</span>
        <% end %>
        <%= logout_link_with_confirm(destroy_user_session_path) %>
    </div>
</nav>
