<%= turbo_stream.replace "program_exercise_#{@exercise.id}" do %>
  <%= render "workout_programs/program_exercise", program: @program, day: @day, pe: @exercise %>
<% end %>

<%= turbo_stream.update "program_exercise_modal_root" %>

<%= turbo_stream.replace "program_day_duration_#{@day.id}" do %>
  <%= render "workout_programs/day_duration_field", day: @day %>
<% end %>
