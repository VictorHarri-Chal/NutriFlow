<% content_for :page_title, "Banque d'aliments" %>

<div class="max-w-6xl mx-auto mt-16 bg-white rounded-xl shadow-md p-8">
    <!-- En-tête avec titre, recherche et actions -->
  <header class="mb-8">
    <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4">
      <!-- Titre -->
      <h1 class="text-2xl font-bold text-gray-900 flex-shrink-0">Mes aliments</h1>

      <!-- Barre de recherche -->
      <div class="flex-1 max-w-md lg:mx-6">
        <%= form_with url: foods_path, method: :get, local: true, class: "relative" do |f| %>
          <% if params[:q].present? %>
            <% params[:q].each do |key, value| %>
              <%= hidden_field_tag "q[#{key}]", value %>
            <% end %>
          <% end %>

          <%= text_field_tag :query, params[:query],
              class: "w-full pl-10 #{params[:query].present? ? 'pr-10' : 'pr-4'} py-1.5 text-sm bg-gray-50 border border-gray-200 rounded-md focus:outline-2 focus:outline-indigo-600 placeholder-gray-400",
              placeholder: "Rechercher un aliment...",
              autocomplete: "off" %>

          <i class="fas fa-search absolute left-3 top-1/2 -translate-y-1/2 text-gray-400 pointer-events-none"></i>

          <% if params[:query].present? %>
            <%= link_to foods_path, class: "absolute right-3 top-1/2 -translate-y-1/2 text-gray-400 hover:text-gray-600 transition-colors" do %>
              <i class="fas fa-times"></i>
            <% end %>
          <% end %>
        <% end %>
      </div>

      <!-- Bouton d'ajout -->
      <%= link_to new_food_path, class: "inline-flex items-center justify-center px-4 py-2 bg-blue-800 hover:bg-blue-600 text-white rounded-lg shadow-sm transition-colors font-medium flex-shrink-0" do %>
        <i class="fas fa-plus mr-2"></i>
        Ajouter un aliment
      <% end %>
    </div>
  </header>

  <!-- Tableau des aliments -->
  <div class="bg-gray-50 rounded-lg overflow-hidden">
    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-gray-200">
        <%= render "table_headers" %>
        <tbody class="bg-white divide-y divide-gray-200">
          <%= render partial: "table_row", collection: @foods, as: :food %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Pagination -->
  <% if @pagy&.pages&.> 1 %>
    <div class="mt-6 flex justify-center">
      <%= render PaginationComponent.new(pagy: @pagy, full_result_option: true) %>
    </div>
  <% elsif @pagy.nil? && params[:full_result] == "true" %>
    <div class="mt-6 flex justify-center">
      <%= render PaginationComponent.new(pagy: nil, full_result_option: true) %>
    </div>
  <% end %>
</div>
